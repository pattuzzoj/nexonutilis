import bootstrap from "./bootstrap.json";
import tailwind from "./tailwind.json";
import less from "./less.json";
import sass from "./sass.json";

export const css = {
  "type": "categories",
  "title": "CSS",
  "description": "CSS is a styling language for web documents like HTML and XML. It defines the visual appearance and layout of web pages, including colors, fonts, and spacing. CSS separates content from design, enhancing the presentation of websites.",
  "items": [
    bootstrap,
    tailwind,
    less,
    sass,
  ],
  "url": "/development/frontend/css",
}